# make sure to get a Steam WebAPI key from
# https://steamcommunity.com/dev/apikey
# using localhost is sufficent as an ip
default:
  apikey: "$API_KEY" 
  collections: ["3102985554"] 
  exclusions: ["MedicineMoodlesSkillRequirements"] 